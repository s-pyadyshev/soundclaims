mixin block-hero(obj)
    .hero&attributes(attributes)
        .container 
            .hero__content(style=( obj.background_img ? "background-image: url(" + obj.background_img + ")" : "background-image: url(images/market.png)"
            ))
                .hero__block
                    .hero__title 
                        | !{obj.title ? obj.title : "Hero title"}
                    .hero__text 
                        p !{obj.text ? obj.text : "Hero text"} 
                    a.button.button--hero(href=obj.button.href ? obj.button.href : "#" ) 
                        span !{obj.button.text}
                        +svg('arrow-right').button__icon
